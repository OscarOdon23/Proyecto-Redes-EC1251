import pandas as pd

datos=pd.read_excel("data_io.xlsx",1)
valores=datos.astype(float)

cantidadValores=valores.loc[:, "Vpico f (V)]
count=0
for i in cantidad de valores
  count +=1
  print(valores.loc[count-1, "Vpico f(V)"]
if all(valores)>0:
    print("Es Positivo")
  else:
    print("Es Negativo")
